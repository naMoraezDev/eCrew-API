extends layout

block content
  h1 #{title} <span>v#{version}</span>
  p Check <a href="#{docs}">#{docs}</a> for full documentation.
  p Also check the <a href="#{repository}">GitHub</a> repository.
  